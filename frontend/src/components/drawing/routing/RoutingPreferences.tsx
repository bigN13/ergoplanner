'use client';\n\nimport React, { useState, useCallback } from 'react';\nimport {\n  RoutingPreferences,\n  RoutingAlgorithm,\n  RoutingConstraint,\n} from '@/types/routing';\nimport { cn } from '@/lib/utils';\n\ninterface RoutingPreferencesProps {\n  preferences: RoutingPreferences;\n  onPreferencesChange: (preferences: RoutingPreferences) => void;\n  onConstraintAdd: (constraint: RoutingConstraint) => void;\n  onConstraintRemove: (constraintId: string) => void;\n  constraints: RoutingConstraint[];\n  readonly?: boolean;\n}\n\n/**\n * RoutingPreferences component provides UI for configuring routing behavior\n * Allows users to set global routing preferences and manage routing constraints\n */\nexport const RoutingPreferencesPanel: React.FC<RoutingPreferencesProps> = ({\n  preferences,\n  onPreferencesChange,\n  onConstraintAdd,\n  onConstraintRemove,\n  constraints,\n  readonly = false,\n}) => {\n  const [activeTab, setActiveTab] = useState<'global' | 'pipes' | 'signals' | 'constraints'>('global');\n  const [isExpanded, setIsExpanded] = useState(false);\n\n  /**\n   * Update global settings\n   */\n  const updateGlobalSettings = useCallback(\n    (updates: Partial<RoutingPreferences['globalSettings']>) => {\n      if (readonly) return;\n      \n      onPreferencesChange({\n        ...preferences,\n        globalSettings: {\n          ...preferences.globalSettings,\n          ...updates,\n        },\n      });\n    },\n    [preferences, onPreferencesChange, readonly]\n  );\n\n  /**\n   * Update pipe defaults\n   */\n  const updatePipeDefaults = useCallback(\n    (updates: Partial<RoutingPreferences['pipeDefaults']>) => {\n      if (readonly) return;\n      \n      onPreferencesChange({\n        ...preferences,\n        pipeDefaults: {\n          ...preferences.pipeDefaults,\n          ...updates,\n        },\n      });\n    },\n    [preferences, onPreferencesChange, readonly]\n  );\n\n  /**\n   * Update signal defaults\n   */\n  const updateSignalDefaults = useCallback(\n    (updates: Partial<RoutingPreferences['signalDefaults']>) => {\n      if (readonly) return;\n      \n      onPreferencesChange({\n        ...preferences,\n        signalDefaults: {\n          ...preferences.signalDefaults,\n          ...updates,\n        },\n      });\n    },\n    [preferences, onPreferencesChange, readonly]\n  );\n\n  /**\n   * Update validation settings\n   */\n  const updateValidationSettings = useCallback(\n    (updates: Partial<RoutingPreferences['validationSettings']>) => {\n      if (readonly) return;\n      \n      onPreferencesChange({\n        ...preferences,\n        validationSettings: {\n          ...preferences.validationSettings,\n          ...updates,\n        },\n      });\n    },\n    [preferences, onPreferencesChange, readonly]\n  );\n\n  /**\n   * Add a new routing constraint\n   */\n  const handleAddConstraint = useCallback(() => {\n    if (readonly) return;\n    \n    const newConstraint: RoutingConstraint = {\n      id: `constraint_${Date.now()}`,\n      type: 'avoid',\n      area: {\n        x: 100,\n        y: 100,\n        width: 100,\n        height: 100,\n      },\n      priority: 1,\n      description: 'New constraint area',\n    };\n    \n    onConstraintAdd(newConstraint);\n  }, [onConstraintAdd, readonly]);\n\n  /**\n   * Remove a routing constraint\n   */\n  const handleRemoveConstraint = useCallback(\n    (constraintId: string) => {\n      if (readonly) return;\n      onConstraintRemove(constraintId);\n    },\n    [onConstraintRemove, readonly]\n  );\n\n  if (!isExpanded) {\n    return (\n      <div className=\"fixed top-4 left-4 z-50\">\n        <button\n          onClick={() => setIsExpanded(true)}\n          className=\"bg-white shadow-lg border border-gray-300 rounded-lg px-3 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50\"\n        >\n          ⚙️ Routing Preferences\n        </button>\n      </div>\n    );\n  }\n\n  return (\n    <div className=\"fixed top-4 left-4 w-80 bg-white shadow-xl border border-gray-300 rounded-lg z-50 max-h-[calc(100vh-2rem)] overflow-hidden flex flex-col\">\n      {/* Header */}\n      <div className=\"flex items-center justify-between p-4 border-b border-gray-200\">\n        <h3 className=\"text-lg font-semibold text-gray-900\">Routing Preferences</h3>\n        <button\n          onClick={() => setIsExpanded(false)}\n          className=\"text-gray-500 hover:text-gray-700 p-1\"\n        >\n          ✕\n        </button>\n      </div>\n\n      {/* Tabs */}\n      <div className=\"flex border-b border-gray-200\">\n        {[\n          { id: 'global', label: 'Global' },\n          { id: 'pipes', label: 'Pipes' },\n          { id: 'signals', label: 'Signals' },\n          { id: 'constraints', label: 'Constraints' },\n        ].map((tab) => (\n          <button\n            key={tab.id}\n            onClick={() => setActiveTab(tab.id as any)}\n            className={cn(\n              'flex-1 py-2 px-3 text-sm font-medium border-b-2 transition-colors',\n              activeTab === tab.id\n                ? 'border-blue-500 text-blue-600'\n                : 'border-transparent text-gray-500 hover:text-gray-700'\n            )}\n          >\n            {tab.label}\n          </button>\n        ))}\n      </div>\n\n      {/* Content */}\n      <div className=\"flex-1 overflow-y-auto p-4 space-y-4\">\n        {/* Global Settings */}\n        {activeTab === 'global' && (\n          <div className=\"space-y-4\">\n            <div>\n              <label className=\"block text-sm font-medium text-gray-700 mb-2\">\n                Default Algorithm\n              </label>\n              <select\n                value={preferences.globalSettings.algorithm}\n                onChange={(e) => updateGlobalSettings({ algorithm: e.target.value as RoutingAlgorithm })}\n                disabled={readonly}\n                className=\"w-full border border-gray-300 rounded-md px-3 py-2 text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500\"\n              >\n                <option value={RoutingAlgorithm.ORTHOGONAL}>Orthogonal (Manhattan)</option>\n                <option value={RoutingAlgorithm.BEZIER}>Bezier Curves</option>\n                <option value={RoutingAlgorithm.STRAIGHT}>Straight Lines</option>\n                <option value={RoutingAlgorithm.DIAGONAL}>Diagonal</option>\n              </select>\n            </div>\n\n            <div className=\"space-y-3\">\n              {[\n                {\n                  key: 'optimization',\n                  label: 'Enable Route Optimization',\n                  description: 'Automatically optimize paths for efficiency',\n                },\n                {\n                  key: 'realTimeRouting',\n                  label: 'Real-time Routing',\n                  description: 'Update routes as nodes are moved',\n                },\n                {\n                  key: 'showCrossings',\n                  label: 'Show Crossings',\n                  description: 'Display crossing indicators',\n                },\n                {\n                  key: 'showWaypoints',\n                  label: 'Show Waypoints',\n                  description: 'Display waypoint handles',\n                },\n                {\n                  key: 'enableAnimations',\n                  label: 'Enable Animations',\n                  description: 'Show flow and signal animations',\n                },\n              ].map((setting) => (\n                <div key={setting.key} className=\"flex items-start space-x-3\">\n                  <input\n                    type=\"checkbox\"\n                    id={setting.key}\n                    checked={preferences.globalSettings[setting.key as keyof typeof preferences.globalSettings] as boolean}\n                    onChange={(e) => updateGlobalSettings({ [setting.key]: e.target.checked })}\n                    disabled={readonly}\n                    className=\"mt-1 h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded\"\n                  />\n                  <div className=\"flex-1\">\n                    <label htmlFor={setting.key} className=\"text-sm font-medium text-gray-700\">\n                      {setting.label}\n                    </label>\n                    <p className=\"text-xs text-gray-500 mt-1\">{setting.description}</p>\n                  </div>\n                </div>\n              ))}\n            </div>\n          </div>\n        )}\n\n        {/* Pipe Settings */}\n        {activeTab === 'pipes' && (\n          <div className=\"space-y-4\">\n            <div>\n              <label className=\"block text-sm font-medium text-gray-700 mb-2\">\n                Default Algorithm\n              </label>\n              <select\n                value={preferences.pipeDefaults.algorithm}\n                onChange={(e) => updatePipeDefaults({ algorithm: e.target.value as RoutingAlgorithm })}\n                disabled={readonly}\n                className=\"w-full border border-gray-300 rounded-md px-3 py-2 text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500\"\n              >\n                <option value={RoutingAlgorithm.ORTHOGONAL}>Orthogonal (Manhattan)</option>\n                <option value={RoutingAlgorithm.BEZIER}>Bezier Curves</option>\n                <option value={RoutingAlgorithm.STRAIGHT}>Straight Lines</option>\n              </select>\n            </div>\n\n            <div>\n              <label className=\"block text-sm font-medium text-gray-700 mb-2\">\n                Bend Radius (px)\n              </label>\n              <input\n                type=\"number\"\n                value={preferences.pipeDefaults.bendRadius}\n                onChange={(e) => updatePipeDefaults({ bendRadius: Number(e.target.value) })}\n                disabled={readonly}\n                min=\"5\"\n                max=\"50\"\n                className=\"w-full border border-gray-300 rounded-md px-3 py-2 text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500\"\n              />\n            </div>\n\n            <div>\n              <label className=\"block text-sm font-medium text-gray-700 mb-2\">\n                Pipe Spacing (px)\n              </label>\n              <input\n                type=\"number\"\n                value={preferences.pipeDefaults.spacing}\n                onChange={(e) => updatePipeDefaults({ spacing: Number(e.target.value) })}\n                disabled={readonly}\n                min=\"10\"\n                max=\"100\"\n                className=\"w-full border border-gray-300 rounded-md px-3 py-2 text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500\"\n              />\n            </div>\n\n            <div className=\"space-y-3\">\n              {[\n                {\n                  key: 'showFlow',\n                  label: 'Show Flow Indicators',\n                  description: 'Display flow direction and animations',\n                },\n                {\n                  key: 'showLabels',\n                  label: 'Show Pipe Labels',\n                  description: 'Display pipe specifications',\n                },\n              ].map((setting) => (\n                <div key={setting.key} className=\"flex items-start space-x-3\">\n                  <input\n                    type=\"checkbox\"\n                    id={`pipe_${setting.key}`}\n                    checked={preferences.pipeDefaults[setting.key as keyof typeof preferences.pipeDefaults] as boolean}\n                    onChange={(e) => updatePipeDefaults({ [setting.key]: e.target.checked })}\n                    disabled={readonly}\n                    className=\"mt-1 h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded\"\n                  />\n                  <div className=\"flex-1\">\n                    <label htmlFor={`pipe_${setting.key}`} className=\"text-sm font-medium text-gray-700\">\n                      {setting.label}\n                    </label>\n                    <p className=\"text-xs text-gray-500 mt-1\">{setting.description}</p>\n                  </div>\n                </div>\n              ))}\n            </div>\n          </div>\n        )}\n\n        {/* Signal Settings */}\n        {activeTab === 'signals' && (\n          <div className=\"space-y-4\">\n            <div>\n              <label className=\"block text-sm font-medium text-gray-700 mb-2\">\n                Default Algorithm\n              </label>\n              <select\n                value={preferences.signalDefaults.algorithm}\n                onChange={(e) => updateSignalDefaults({ algorithm: e.target.value as RoutingAlgorithm })}\n                disabled={readonly}\n                className=\"w-full border border-gray-300 rounded-md px-3 py-2 text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500\"\n              >\n                <option value={RoutingAlgorithm.STRAIGHT}>Straight Lines</option>\n                <option value={RoutingAlgorithm.ORTHOGONAL}>Orthogonal (Manhattan)</option>\n                <option value={RoutingAlgorithm.BEZIER}>Bezier Curves</option>\n              </select>\n            </div>\n\n            <div>\n              <label className=\"block text-sm font-medium text-gray-700 mb-2\">\n                Signal Separation (px)\n              </label>\n              <input\n                type=\"number\"\n                value={preferences.signalDefaults.separation}\n                onChange={(e) => updateSignalDefaults({ separation: Number(e.target.value) })}\n                disabled={readonly}\n                min=\"5\"\n                max=\"50\"\n                className=\"w-full border border-gray-300 rounded-md px-3 py-2 text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500\"\n              />\n            </div>\n\n            <div className=\"space-y-3\">\n              {[\n                {\n                  key: 'bundling',\n                  label: 'Enable Cable Bundling',\n                  description: 'Group compatible signals together',\n                },\n                {\n                  key: 'showProtocol',\n                  label: 'Show Protocol Labels',\n                  description: 'Display signal protocol information',\n                },\n              ].map((setting) => (\n                <div key={setting.key} className=\"flex items-start space-x-3\">\n                  <input\n                    type=\"checkbox\"\n                    id={`signal_${setting.key}`}\n                    checked={preferences.signalDefaults[setting.key as keyof typeof preferences.signalDefaults] as boolean}\n                    onChange={(e) => updateSignalDefaults({ [setting.key]: e.target.checked })}\n                    disabled={readonly}\n                    className=\"mt-1 h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded\"\n                  />\n                  <div className=\"flex-1\">\n                    <label htmlFor={`signal_${setting.key}`} className=\"text-sm font-medium text-gray-700\">\n                      {setting.label}\n                    </label>\n                    <p className=\"text-xs text-gray-500 mt-1\">{setting.description}</p>\n                  </div>\n                </div>\n              ))}\n            </div>\n          </div>\n        )}\n\n        {/* Constraints */}\n        {activeTab === 'constraints' && (\n          <div className=\"space-y-4\">\n            <div className=\"flex items-center justify-between\">\n              <h4 className=\"text-sm font-medium text-gray-700\">Routing Constraints</h4>\n              <button\n                onClick={handleAddConstraint}\n                disabled={readonly}\n                className=\"bg-blue-500 text-white px-3 py-1 rounded text-sm hover:bg-blue-600 disabled:opacity-50\"\n              >\n                Add Constraint\n              </button>\n            </div>\n\n            <div className=\"space-y-2 max-h-40 overflow-y-auto\">\n              {constraints.map((constraint) => (\n                <div\n                  key={constraint.id}\n                  className=\"flex items-center justify-between p-3 bg-gray-50 rounded-lg border\"\n                >\n                  <div className=\"flex-1\">\n                    <div className=\"text-sm font-medium text-gray-700\">\n                      {constraint.description || 'Unnamed constraint'}\n                    </div>\n                    <div className=\"text-xs text-gray-500\">\n                      Type: {constraint.type} | Priority: {constraint.priority}\n                    </div>\n                    {constraint.area && (\n                      <div className=\"text-xs text-gray-500\">\n                        Area: {constraint.area.width}×{constraint.area.height} at ({constraint.area.x}, {constraint.area.y})\n                      </div>\n                    )}\n                  </div>\n                  <button\n                    onClick={() => handleRemoveConstraint(constraint.id)}\n                    disabled={readonly}\n                    className=\"text-red-500 hover:text-red-700 p-1 disabled:opacity-50\"\n                  >\n                    🗑️\n                  </button>\n                </div>\n              ))}\n            </div>\n\n            {constraints.length === 0 && (\n              <div className=\"text-center py-8 text-gray-500\">\n                <p className=\"text-sm\">No routing constraints defined</p>\n                <p className=\"text-xs mt-1\">Add constraints to control routing behavior</p>\n              </div>\n            )}\n          </div>\n        )}\n\n        {/* Validation Settings */}\n        <div className=\"border-t border-gray-200 pt-4\">\n          <h4 className=\"text-sm font-medium text-gray-700 mb-3\">Validation Settings</h4>\n          <div className=\"space-y-3\">\n            {[\n              {\n                key: 'enableRealTime',\n                label: 'Real-time Validation',\n                description: 'Validate connections as they are created',\n              },\n              {\n                key: 'strictMode',\n                label: 'Strict Mode',\n                description: 'Enforce strict engineering rules',\n              },\n              {\n                key: 'autoCorrect',\n                label: 'Auto-correct',\n                description: 'Automatically fix simple issues',\n              },\n              {\n                key: 'highlightIssues',\n                label: 'Highlight Issues',\n                description: 'Visually highlight validation problems',\n              },\n            ].map((setting) => (\n              <div key={setting.key} className=\"flex items-start space-x-3\">\n                <input\n                  type=\"checkbox\"\n                  id={`validation_${setting.key}`}\n                  checked={preferences.validationSettings[setting.key as keyof typeof preferences.validationSettings] as boolean}\n                  onChange={(e) => updateValidationSettings({ [setting.key]: e.target.checked })}\n                  disabled={readonly}\n                  className=\"mt-1 h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded\"\n                />\n                <div className=\"flex-1\">\n                  <label htmlFor={`validation_${setting.key}`} className=\"text-sm font-medium text-gray-700\">\n                    {setting.label}\n                  </label>\n                  <p className=\"text-xs text-gray-500 mt-1\">{setting.description}</p>\n                </div>\n              </div>\n            ))}\n          </div>\n        </div>\n      </div>\n\n      {/* Footer */}\n      <div className=\"border-t border-gray-200 p-4\">\n        <div className=\"text-xs text-gray-500\">\n          Changes are applied immediately to new connections\n        </div>\n      </div>\n    </div>\n  );\n};\n\nexport default RoutingPreferencesPanel;\n"