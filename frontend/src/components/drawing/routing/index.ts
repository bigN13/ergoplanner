/**\n * Edge Routing System - Index file\n * \n * This module provides comprehensive edge routing capabilities for P&ID drawings.\n * It includes intelligent pathfinding, collision detection, manual adjustment tools,\n * and performance optimizations for complex layouts.\n * \n * Key Features:\n * - Orthogonal (Manhattan) routing with collision detection\n * - Multiple edge types: Pipes, Signals, Annotations\n * - Manual waypoint adjustment system\n * - Real-time connection validation\n * - Flow direction indicators and animations\n * - Crossing detection and management\n * - Performance optimizations for large drawings\n * - Engineering rule validation\n * - Pipe sizing and specification management\n */\n\n// Core routing components\nexport { default as RoutingManager, useRoutingManager } from './RoutingManager';\nexport { default as RoutingPreferencesPanel } from './RoutingPreferences';\n\n// Edge components\nexport { default as PipeEdge, createPipeEdgeData } from '../edges/PipeEdge';\nexport { default as SignalEdge, createSignalEdgeData } from '../edges/SignalEdge';\nexport { default as AnnotationEdge, createAnnotationEdgeData } from '../edges/AnnotationEdge';\n\n// Waypoint management\nexport { default as WaypointManager } from '../waypoints/WaypointManager';\n\n// Routing utilities\nexport { OrthogonalRouter, createOrthogonalRouter } from '../../utils/routing/orthogonalRouter';\nexport { \n  validateConnection,\n  validateConnections,\n  getDefaultValidationRules,\n  createConnectionValidator,\n  ConnectionValidator,\n} from '../../utils/routing/connectionValidator';\nexport {\n  detectEdgeCrossings,\n  analyzeCrossingConflicts,\n  createCrossingManager,\n  CrossingManager,\n} from '../../utils/routing/crossingManager';\nexport {\n  calculatePipeSpecifications,\n  createDefaultEdgeData,\n  generateEdgeId,\n  getEdgeStyle,\n  canBundleEdges,\n  calculateBundleSpacing,\n  optimizeEdgeCrossings,\n} from '../../utils/routing/routingUtils';\nexport {\n  createPerformanceOptimizer,\n  batchProcessRouting,\n  createRoutingDebouncer,\n  RoutingWorker,\n  PerformanceOptimizer,\n} from '../../utils/routing/performanceOptimizer';\n\n// Types\nexport type {\n  // Core routing types\n  RoutingEdge,\n  EdgeData,\n  PipeEdgeData,\n  SignalEdgeData,\n  AnnotationEdgeData,\n  RoutingResult,\n  RoutingContext,\n  \n  // Routing configuration\n  RoutingConfig,\n  RoutingPreferences,\n  RoutingConstraint,\n  \n  // Waypoints and path management\n  Waypoint,\n  PathSegment,\n  \n  // Validation\n  ValidationRule,\n  ConnectionValidation,\n  ValidationError,\n  \n  // Crossings\n  EdgeCrossing,\n  Conflict,\n  \n  // Performance\n  RoutingPerformance,\n  \n  // Pipe specifications\n  PipeSpecification,\n  \n  // Edge types and enums\n  EdgeType,\n  PipeType,\n  SignalType,\n  RoutingAlgorithm,\n  FlowDirection,\n  CrossingType,\n} from '../../types/routing';\n\n// Redux selectors and actions\nexport {\n  // Selectors\n  selectRoutingState,\n  selectRoutingPreferences,\n  selectRoutingConstraints,\n  selectEdgeCrossings,\n  selectValidationErrors,\n  selectIsRoutingEnabled,\n  selectRoutingMode,\n  selectShowWaypoints,\n  selectShowCrossings,\n  selectRealTimeRouting,\n  selectRoutingEdges,\n  selectEdgeWaypoints,\n  selectEdgeValidation,\n  selectLastRoutingUpdate,\n  selectEdgeWaypointsById,\n  selectEdgeValidationById,\n  selectCrossingsByEdgeId,\n  selectValidationErrorCount,\n  selectRoutingStatistics,\n  \n  // Actions\n  updateRoutingPreferences,\n  setRoutingMode,\n  toggleRoutingEnabled,\n  addRoutingConstraint,\n  updateRoutingConstraint,\n  removeRoutingConstraint,\n  setCrossings,\n  addCrossing,\n  removeCrossing,\n  updateCrossing,\n  setEdgeWaypoints,\n  updateWaypoint,\n  removeWaypoint,\n  setEdgeValidation,\n  clearEdgeValidation,\n  toggleShowWaypoints,\n  toggleShowCrossings,\n  setRealTimeRouting,\n  addRoutingEdge,\n  updateRoutingEdge,\n  removeRoutingEdge,\n  clearAllRouting,\n  updateRoutingTimestamp,\n  \n  // Action creators\n  routingActions,\n} from '../../store/slices/drawingSlice';\n\n/**\n * Default routing configuration for P&ID drawings\n */\nexport const DEFAULT_ROUTING_CONFIG = {\n  algorithm: 'orthogonal' as const,\n  minSegmentLength: 20,\n  maxBends: 8,\n  bendRadius: 10,\n  gridAlignment: true,\n  avoidObstacles: true,\n  spacing: {\n    parallel: 30,\n    crossing: 20,\n    node: 40,\n  },\n  optimization: {\n    minimizeLength: true,\n    minimizeBends: true,\n    avoidCrossings: true,\n    maintainFlow: true,\n  },\n};\n\n/**\n * Default routing preferences\n */\nexport const DEFAULT_ROUTING_PREFERENCES = {\n  globalSettings: {\n    algorithm: 'orthogonal' as const,\n    optimization: true,\n    realTimeRouting: true,\n    showCrossings: true,\n    showWaypoints: true,\n    enableAnimations: true,\n  },\n  pipeDefaults: {\n    algorithm: 'orthogonal' as const,\n    bendRadius: 10,\n    spacing: 30,\n    showFlow: true,\n    showLabels: true,\n  },\n  signalDefaults: {\n    algorithm: 'straight' as const,\n    separation: 15,\n    bundling: true,\n    showProtocol: true,\n  },\n  validationSettings: {\n    enableRealTime: true,\n    strictMode: false,\n    autoCorrect: false,\n    highlightIssues: true,\n  },\n};\n\n/**\n * Edge type registry for dynamic edge creation\n */\nexport const EDGE_TYPE_REGISTRY = {\n  pipe: {\n    component: 'PipeEdge',\n    dataFactory: 'createPipeEdgeData',\n    defaultProps: {\n      animated: false,\n      type: 'pipe',\n    },\n  },\n  signal: {\n    component: 'SignalEdge',\n    dataFactory: 'createSignalEdgeData',\n    defaultProps: {\n      animated: false,\n      type: 'signal',\n    },\n  },\n  annotation: {\n    component: 'AnnotationEdge',\n    dataFactory: 'createAnnotationEdgeData',\n    defaultProps: {\n      animated: false,\n      type: 'annotation',\n    },\n  },\n};\n\n/**\n * Validation rule categories\n */\nexport const VALIDATION_CATEGORIES = {\n  CONNECTION: 'connection',\n  FLOW: 'flow',\n  PRESSURE: 'pressure',\n  TEMPERATURE: 'temperature',\n  MATERIAL: 'material',\n  SIGNAL: 'signal',\n  PROTOCOL: 'protocol',\n} as const;\n\n/**\n * Performance thresholds for optimization recommendations\n */\nexport const PERFORMANCE_THRESHOLDS = {\n  MAX_RENDER_TIME: 100, // ms\n  MAX_EDGES_WITHOUT_VIRTUALIZATION: 500,\n  MAX_MEMORY_USAGE: 10 * 1024 * 1024, // 10MB\n  MIN_CACHE_HIT_RATIO: 0.8,\n  MAX_MANUAL_ROUTING_RATIO: 0.5,\n} as const;\n\n/**\n * Utility function to create a complete routing setup\n */\nexport function createRoutingSetup(options: {\n  preferences?: Partial<typeof DEFAULT_ROUTING_PREFERENCES>;\n  config?: Partial<typeof DEFAULT_ROUTING_CONFIG>;\n  enablePerformanceOptimization?: boolean;\n} = {}) {\n  const {\n    preferences = {},\n    config = {},\n    enablePerformanceOptimization = true,\n  } = options;\n\n  const mergedPreferences = {\n    ...DEFAULT_ROUTING_PREFERENCES,\n    ...preferences,\n  };\n\n  const mergedConfig = {\n    ...DEFAULT_ROUTING_CONFIG,\n    ...config,\n  };\n\n  const router = createOrthogonalRouter(mergedConfig);\n  const validator = createConnectionValidator();\n  const crossingManager = createCrossingManager();\n  const optimizer = enablePerformanceOptimization \n    ? createPerformanceOptimizer() \n    : null;\n\n  return {\n    router,\n    validator,\n    crossingManager,\n    optimizer,\n    preferences: mergedPreferences,\n    config: mergedConfig,\n  };\n}\n\n/**\n * Version information\n */\nexport const ROUTING_SYSTEM_VERSION = '1.0.0';\nexport const ROUTING_SYSTEM_BUILD = Date.now();\n\n/**\n * System information and capabilities\n */\nexport const ROUTING_SYSTEM_INFO = {\n  version: ROUTING_SYSTEM_VERSION,\n  build: ROUTING_SYSTEM_BUILD,\n  features: [\n    'Orthogonal routing with collision detection',\n    'Multiple edge types (Pipe, Signal, Annotation)',\n    'Manual waypoint adjustment',\n    'Real-time validation',\n    'Flow direction indicators',\n    'Crossing detection and management',\n    'Performance optimizations',\n    'Engineering rule validation',\n    'Pipe sizing calculations',\n    'Redux state management',\n  ],\n  supportedStandards: [\n    'ISA-5.1',\n    'ISO 14617',\n    'UK Water Industry Standards',\n  ],\n  maxRecommendedEdges: 1000,\n  maxRecommendedWaypoints: 10000,\n};\n"